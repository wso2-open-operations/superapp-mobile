# Base path (must include /admin-portal) used if explicit upload URL not set.
# Example:
REACT_APP_PAYSLIP_API_BASE=<payslip-api-base-including-/admin-portal>

# Absolute upload URL (takes precedence). Recommended to avoid ambiguity.
# Example:
REACT_APP_PAYSLIP_UPLOAD_URL=<payslip-upload-url>

# Absolute micro-app upload URL (used directly by UploadMicroApp component)
# Example:
REACT_APP_MICROAPPS_UPLOAD_URL=<micro-apps-upload-url>

# Base API URL for admin portal backend (no trailing slash)
# Example:
REACT_APP_API_BASE_URL=<api-base-url>

# Optional endpoint-specific overrides
# Example:
REACT_APP_MICROAPPS_LIST_URL=<microapps-list-url>
REACT_APP_USERS_BASE_URL=<users-base-url>
REACT_APP_USERS_URL=<users-url>

# Local dev proxy (optional): configure CRA proxy rewrites without code edits
# Payslip upload proxy target origin and path (if you call /upload locally)
DEV_PROXY_PAYSLIP_TARGET_ORIGIN=<payslip-upload-proxy-target-origin>
DEV_PROXY_PAYSLIP_UPLOAD_PATH=<payslip-upload-proxy-upload-path>
DEV_PROXY_PAYSLIP_UPLOAD_URL=<payslip-upload-proxy-upload-url>

# Micro-apps proxy pieces (used when calling locally)
# If REACT_APP_API_BASE_URL is set, origin and base path will derive from it automatically.
DEV_PROXY_MICROAPPS_TARGET_ORIGIN=<microapps-proxy-target-origin>
DEV_PROXY_MICROAPPS_BASE_PATH=<microapps-proxy-base-path>
DEV_PROXY_MICROAPPS_UPLOAD_PATH=<microapps-proxy-upload-path>

# Proxy logging configuration (dev server)
# Acceptable values: debug | info | warn | error | silent
# PROXY_LOG_LEVEL acts as the default for all proxies.
# MICROAPPS_PROXY_LOG_LEVEL overrides specifically for /api/microapps proxy.
PROXY_LOG_LEVEL=debug
MICROAPPS_PROXY_LOG_LEVEL=<debug|info|warn|error|silent>
